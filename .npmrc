# Fail installs when the active Node version doesn't satisfy "engines".
engine-strict=true

# Let Corepack manage the pnpm version pinned in package.json#packageManager.
manage-package-manager-versions=true
